/**
 * Canonical site origin. The bare getyessay.com 308-redirects to www, and
 * www is the Production deployment, so always build absolute URLs from this.
 */
export const baseUrl = "https://www.getyessay.com";
